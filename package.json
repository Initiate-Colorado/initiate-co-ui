{
    "name": "example-create-react-app-express",
    "version": "1.0.0",
    "scripts": {
        "build": "cd client ",
        "hit-it": "cd api && node server.js && cd .. && cd client && npm start",
        "client": "cd client && npm start",
        "server": "nodemon server.js",
        "dev": "concurrently --kill-others-on-fail \"npm server\" \"npm client\""
    },
    "dependencies": {
        "body-parser": "^1.19.0",
        "express": "^4.17.1"
    },
    "devDependencies": {
        "concurrently": "^4.0.1"
    }
}
