import request from 'supertest'
import express from 'express'
import schema from '../../setup/schema'
import subscriber from './model'

describe('subscriber models', () => {
  
  it('should test that true === true', () => {
    expect(true).toBe(true)

  })
})